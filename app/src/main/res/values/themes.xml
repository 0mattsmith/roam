<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Roam" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/surface</item>
        <item name="android:navigationBarColor">@color/surface</item>
        <item name="android:windowBackground">@color/surface</item>
    </style>

    <!-- Referenced by com.google.android.gms.car.application.theme.
         Android Auto reads the accent from here. -->
    <style name="Theme.Roam.Car" parent="android:Theme.Material">
        <item name="android:colorPrimary">@color/teal_core</item>
        <item name="android:colorPrimaryDark">@color/ink</item>
        <item name="android:colorAccent">@color/teal</item>
    </style>
</resources>
